/** @type {import('next').NextConfig} */
const nextConfig = {
      images: {
            domains: ['www.pokemon.com' , 'img.pokemondb.net' , 'raw.githubusercontent.com'],
          },
};

export default nextConfig;
